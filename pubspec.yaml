name: v2ray_stk
description: V2RAY stk - VPN Client
publish_to: 'none'
version: 1.0.34+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.2
  flutter_v2ray_client: ^3.2.0
  provider: ^6.0.0
  flutter_svg: ^2.0.17
  url_launcher: ^6.3.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
